# Create a lambda function called rapid it will take one string parameter.
# Return the same string with the last letter removed.
# Example:
# rapid("hello") # Outputs: "hell
rapid = lambda my_string : my_string[:-1]
print(rapid("1234"))