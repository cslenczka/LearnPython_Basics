# Write a function called sort_names that, given a list of names, returns them in alphabetical order.

names = ['John', 'Kenny', 'Tom', 'Bob', 'Dilan']

def sorted_list(names_list):
    return sorted(names_list)

print(sorted_list(names))